---
 - hosts: prod
   tasks:
   - name: pull docker images from registry
     raw: docker pull sreedevops26/mydevops:1.0

   - name: bringup container
     raw: docker run -dti --name webapp -p 80:80 sreedevops26/mydevops:1.0 /bin/bash 
